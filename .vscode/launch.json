{
    "configurations": [
        {
            "name": "Docker .NET Core Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "netCore": {
                "appProject": "${workspaceFolder}/src/WebUI/WebUI.csproj"
            },
            "dockerServerReadyAction": {
                "uriFormat": "%s://localhost:%s/api"
            }
        }
    ]
}